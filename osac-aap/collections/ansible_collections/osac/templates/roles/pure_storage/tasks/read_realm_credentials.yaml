---
# Placeholder. Real Realm credential lookup lands with OSAC-5017.
#
# Internal helper, not one of the four dispatched actions. Resolves the Realm's
# API token from the admin-pre-created hub Secret named by its PURE_REALM_POOL
# entry.

- name: Report that pure_storage read_realm_credentials is not yet implemented
  ansible.builtin.debug:
    msg: >-
      pure_storage read_realm_credentials is a placeholder — reading Realm
      credentials from the hub Secret is implemented by OSAC-5017.
